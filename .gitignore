# ==== wp-content/.gitignore ====

# 1) התעלם מהכול כברירת מחדל
/*

# 2) הרשה קבצים/תיקיות בסיסיים בשורש
!.gitignore
!README.md
!.gitattributes
!.editorconfig

# 3) הרשה תיקיות שתרצה לעקוב אחריהן (עדיין בלי התוכן)
!plugins/
!themes/
!mu-plugins/
!OLD\ MU/                     # עוקב אחרי wp-content/OLD MU

# 4) PLUGINS — עקוב רק אחרי הפלאגין שלך
plugins/*
!plugins/tapin-next/
!plugins/tapin-next/**

# אם תוסיף פלאגין משלך בעתיד:
# !plugins/tapin-foo/
# !plugins/tapin-foo/**

# 5) THEMES — אם יש תבנית מותאמת, בטל הערות ועדכן שם
themes/*
# !themes/tapin-theme/
# !themes/tapin-theme/**

# 6) MU-PLUGINS — אפשר קבצי MU משלך (tapin*) וקבצי PHP בודדים
mu-plugins/*
!mu-plugins/*.php
!mu-plugins/tapin*/
!mu-plugins/tapin*/**

# 7) OLD MU — עקוב אחרי כל התוכן שבתיקייה זו (עם רווח בשם)
!OLD\ MU/**
# הערה: כללי ignore גלובליים למטה (כמו *.zip/*.sql) עדיין יחולו גם בתוך OLD MU

# 8) כללי — מדיה/גיבויים/קאש/לוגים/ארטיפקטים
uploads/**
cache/**
upgrade*/**
ai1wm-backups/**
backup-*/**
backuply/**
wp/**

languages/**
speedycache-config/**

debug.log
*.log
*.tmp
*.swp

# ארכיונים/דאמפים (יישארו מחוץ לריפו גם בתוך OLD MU)
*.sql
*.sqlite
*.zip
*.tar
*.gz
*.7z

# קבצי build
node_modules/
vendor/
dist/
build/
coverage/

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
