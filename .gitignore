# ==== wp-content/.gitignore ====

# התעלם מהכול כברירת מחדל
/*

# הרשה קבצים/תיקיות בסיסיים בשורש
!.gitignore
!README.md
!.gitattributes
!.editorconfig

# הרשה את תיקיות הקוד (עדיין בלי התוכן)
!plugins/
!themes/
!mu-plugins/
!OLD MU/                  # ← תיקיית OLD MU בשורש wp-content

# PLUGINS — עקוב רק אחרי הפלאגין שלך
plugins/*
!plugins/tapin-next/
!plugins/tapin-next/**

# THEMES — אם יש תבנית משלך, בטל הערה ועדכן שם
themes/*
# !themes/tapin-theme/
# !themes/tapin-theme/**

# MU-PLUGINS — אפשר קבצי MU משלך (tapin*) וקבצי PHP בודדים
mu-plugins/*
!mu-plugins/*.php
!mu-plugins/tapin*/
!mu-plugins/tapin*/**

# OLD MU — עקוב אחרי כל התוכן שבתיקייה זו
!OLD MU/**
# שים לב: החסימות הכלליות למטה (zip/sql וכו') עדיין חלות.
# אם תרצה להעלות גם ארכיונים/SQL בתוך OLD MU, ראה הבלוק האופציונלי בסוף.

# כלליים — מדיה/גיבויים/קאש/לוגים/ארטיפקטים
uploads/**
cache/**
upgrade*/**
ai1wm-backups/**
backup-*/**
backuply/**
wp/**

languages/**
speedycache-config/**

debug.log
*.log
*.tmp
*.swp
*.sql
*.sqlite
*.zip
*.tar
*.gz
*.7z

node_modules/
vendor/
dist/
build/
coverage/

.vscode/
.idea/
.DS_Store
Thumbs.db

# (אופציונלי) אם אתה חייב ש־.zip/.sql בתוך OLD MU כן ייכנסו:
# בטל את ההערות של שתי השורות הבאות:
# !OLD MU/**/*.zip
# !OLD MU/**/*.sql
